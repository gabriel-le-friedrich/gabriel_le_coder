{
  "name": "asf-functions",
  "description": "ASF Firebase Cloud Functions — stamps the role:authenticated custom claim onto every Firebase user, which is what lets Supabase's Firebase Third-Party Auth integration grant the `authenticated` Postgres role instead of defaulting to `anon`. See ../FIREBASE_THIRDPARTY_AUTH_SETUP.md for the full setup this is one piece of.",
  "engines": {
    "node": "20"
  },
  "main": "index.js",
  "scripts": {
    "deploy": "firebase deploy --only functions",
    "backfill": "node backfill-claims.js"
  },
  "dependencies": {
    "firebase-admin": "^12.6.0",
    "firebase-functions": "^5.1.1"
  },
  "private": true
}
